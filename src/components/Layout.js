import { Outlet } from "react-router-dom"

const Layout = () => {
    return (
        <main className="App">
            {/* <Outlet /> */}
            <div>
                <h2>This is main page....!!!</h2>
            </div>
        </main>
    )
}

export default Layout